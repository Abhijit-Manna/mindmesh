[project]
name = "mindmesh"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rahul Singh", email = "singhrahul161104@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "crewai-tools>=1.15.22",
    "crewai[litellm,tools]>=1.15.22",
    "streamlit>=1.64.0",
]

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["backend"]
